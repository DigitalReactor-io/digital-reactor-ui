/**
 * Created by MStepachev on 07.09.2016.
 */
define(
    [
        "backbone",
        "text!templates/registration/progressBar.html"
    ],
    function (Backbone, ProgressBarTemplate) {
        var ProgressBar = Backbone.View.extend(
            
        );
        
        return ProgressBar;
    }
);